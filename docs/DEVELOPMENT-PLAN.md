# DSA Development Plan

**Current baseline:** Kiwe DSA `0.6.22`, WordPress 7.x, PHP 8.2-8.4, Bricks 2.3.7-first, shared-host compatible.

This is the authoritative short execution view. `DSA-ARCHITECTURE.md` remains the architectural history and decision record. `SECURITY-AUDIT.md` owns security acceptance detail.

## Implemented AI Track - Kiwe Studio AI + Companion + SecureTrack AI Settings

Status: Companion and SecureTrack relocation landed in `0.6.11`; Studio operating modes and native-provider controls landed in `0.6.12`; Bricks AI Intelligence and the optional Bricks front-end editor companion landed in `0.6.13`; SecureTrack now uses shared Companion/native AI settings without a separate SecureTrack key field as of `0.6.14`; AI access key creation has a shared-host-safe one-time-secret display fallback as of `0.6.15`; AI-less Site Graph Data now supports both explicit `queries` and compact `resources` batch reads as of `0.6.16`; combined-mode preview/live parity and native prompt compaction were tightened in `0.6.17`; sheet grabber chrome anchoring landed in `0.6.18`; Companion review parity tightening landed in `0.6.19`; sheet grabber chrome metrics were tokenized in `0.6.20`; the first Seam token-hygiene audit and dock/search token fixes landed in `0.6.21`; strict runtime token-purity enforcement landed in `0.6.22`. Keep this as the operating boundary for later deeper WordPress AI Client and model-orchestration work.

Product direction:

- Move all AI-facing SecureTrack settings out of `Kiwe > Secure` and into `Kiwe > AI`.
- Keep `Kiwe > Secure` focused on security enforcement, auto logout, risk rules, break-glass, monitoring, and human security controls.
- `Kiwe > AI` should own API keys, AI scopes, Studio operating mode, native-provider settings, companion access, redacted SecureTrack AI brief permissions, advisor/enrichment settings, connector budgets, and AI audit logs. SecureTrack must not render a separate provider/model/API-key lane; it uses Companion consent/scopes and the shared Native AI provider when supported.

Key principle:

The same revocable Kiwe AI key can be allowed to access SecureTrack AI context only when the admin explicitly grants that scope. Otherwise the key may still work for normal AI connector features such as Site Graph, theme package install/review, framework profile application, staging proof metadata, and dynamic binding support. Deleting the key in `Kiwe > AI` must revoke all access immediately.

Long-term vision:

Create an optional **Kiwe Studio AI** inside the plugin. When a designer/developer uses external browser AI, IDE AI, GitHub/tool calls, or native Kiwe AI to create a website/page, DSA/AppShell theme, or combined handoff, Studio chooses the cheapest safe lane: native Kiwe AI with an admin-provided key, browser AI plus Companion packets, or browser AI only. The companion should make the external AI's work easier without forcing it to read the whole plugin or wasting tokens.

Architecture intent:

- Studio is a workflow engine; the companion is a site-aware context broker and reviewer, not an always-chatty chatbot.
- Deterministic systems come first: Site Graph, Site Graph Data, framework/theme schemas, SecureTrack redacted brief, installed plugin inventory, Bricks capability summaries, validation rules, previous audit failures, and safe settings state.
- Model calls, when available through WordPress 7 AI Client / native AI abilities, are optional enrichment over a compact context packet, not the source of authority.
- Companion responses should be small, hashable, cached, and delta-based: "read this context", "these are the missing selectors", "this theme CSS owns dock geometry", "this page needs real query loops", "these categories/products exist", etc.
- Avoid token waste by returning context cards, rule IDs, validation diffs, and short next-action plans instead of dumping full files or logs.
- Treat the "learning" layer as local product memory, not uncontrolled training: store anonymized/structured pass-fail fingerprints, recurring handoff mistakes, accepted fixes, validator error frequencies, and site-specific preferences. Do not store raw secrets, full visitor trails, raw SecureTrack events, private customer data, or unredacted prompts.

Current endpoints / abilities:

- `GET /wp-json/dsa/v1/ai/companion/status` - feature flags, model/native-AI availability, budgets, scopes, and safe operating boundary.
- `GET /wp-json/dsa/v1/ai/companion/context` - compact mode-aware context packet for website, theme, combined, dynamic binding, or audit work.
- `POST /wp-json/dsa/v1/ai/companion/ask` - bounded question-answer interface over approved context only.
- `POST /wp-json/dsa/v1/ai/companion/review-output` - non-mutating review of a handoff/package against current Kiwe rules and target-site graph.
- `GET /wp-json/dsa/v1/ai/companion/memory` - admin-readable summary of learned local patterns, stored as safe rule cards rather than raw transcripts.
- `GET /wp-json/dsa/v1/ai/studio/status` - Studio mode, native-provider status, budgets, scopes, and route map.
- `POST /wp-json/dsa/v1/ai/studio/start` - token-saving Studio packet and workflow for browser AI, native AI, or browser-only use.
- `POST /wp-json/dsa/v1/ai/studio/draft` - optional bounded native-provider draft; requires Kiwe > AI native generation and a key with `native_ai` scope.
- `POST /wp-json/dsa/v1/ai/studio/review` - deterministic Companion review gate for the Studio output.
- `GET|POST /wp-json/dsa/v1/ai/bricks/context` - read-only Bricks-native intelligence packet for external AI tools: elements, compact controls, query loops, dynamic tags, conditions, interactions, Seam rules, and Kiwe launcher/runtime boundaries.
- `POST /wp-json/dsa/v1/ai/bricks/plan` - compact Bricks + Seam planning packet for website/page, combined, dynamic, or audit work.
- Logged-in Bricks editor routes under `/wp-json/dsa/v1/bricks/studio/*` mirror the context/start/draft flow for the optional front-end editor companion, using WordPress nonce/capabilities rather than API keys.
- WordPress 7 abilities may mirror these as `dsa/get-companion-context`, `dsa/ask-companion`, `dsa/review-ai-output`, `dsa/start-studio-project`, `dsa/review-studio-output`, `dsa/get-bricks-ai-context`, and `dsa/plan-bricks-ai-page` when available.

Suggested admin shape under `Kiwe > AI`:

- API access keys and scopes.
- Studio AI enable/disable and operating mode: `native`, `browser_companion`, or `browser_only`.
- Bricks front-end editor companion enable/disable; this only exposes read-only planning/native-draft assistance in the editor and does not save Bricks content.
- Native AI provider, model, base URL, encrypted API key, max context bytes, max output tokens, and explicit native-generation allow switch.
- Companion AI enable/disable.
- Allowed companion modes: website/page, AppShell theme, combined, dynamic binding, audit, staging proof, SecureTrack brief.
- SecureTrack AI access: off by default; redacted brief only; no enforcement mutation.
- Token/budget controls: max context size, max review size, cache TTL, per-key rate limits.
- Memory controls: enable/disable local learning, clear memory, export memory summary, never-store sensitive categories.
- Audit log: key used, mode, context hash, redaction status, actions suggested, mutations blocked/allowed.

Mutation boundary:

The companion must not save Bricks, publish WordPress content, mutate WooCommerce, run checkout/cart/auth, change SecureTrack enforcement, or alter keys/settings unless the request enters the existing controlled staging executor with explicit human confirmation. The companion can advise, validate, prepare plans, stage proof artifacts, and explain why a proposed handoff failed.

Token hardening backlog:

- Audit remaining historical fixed values in `assets/css/surface.css` and classify them as source-of-truth base tokens, browser fallback values, or component-level magic numbers.
- Move component-level spacing, sizing, radius, shadow, and motion values behind named Kiwe/Seam/DSA tokens before treating any AppShell theme as fully token-pure.
- Keep the boundary precise: numeric values may exist at the base-token/fallback layer, but runtime components and installed themes should consume named tokens instead of embedding one-off values.

Acceptance target for the later batch:

- All AI-related SecureTrack controls appear under `Kiwe > AI`, while `Kiwe > Secure` remains security-control focused.
- API keys support explicit companion/SecureTrack/Studio/native scopes.
- Companion endpoints return compact, redacted, mode-aware packets.
- Studio endpoints return token-saving packets, native draft envelopes, and deterministic review gates without mutation authority.
- Bricks AI endpoints return enough Bricks-native element/query/dynamic/condition/interaction context for browser AI and native AI to plan Bricks pages without crawling Bricks source.
- The Bricks front-end editor companion can fetch context, generate a plan, or request bounded native draft output from inside the editor without directly mutating the page.
- External AIs can use the companion to improve first-pass website/theme/combined outputs without reading the full codebase.
- The companion learns locally from validation/audit results as structured rule cards, not as raw private data.
- Existing Site Graph, dynamic binding, theme package, framework profile, advisor, enrichment, and controlled executor routes remain compatible.

## Active Lead Log - 2026-07-14

Batch 1 of the marketplace UI-brain track is complete as a contract-only addition. The new portable UI system folder lives at `wp-content/mu-plugins/dsa/ui-system/` and captures screen payloads, stable slots, Bricks hooks/dynamic tags, token aliases, budgets, and an example Prototype 2027 profile adapter. It is deliberately not imported by production runtime yet, so the currently working Legacy and Prototype 2027 UI paths are unchanged. Per deployment direction, no version bump, package rebuild, or manifest rehash is required until the final integration batch.

Batch 2 begins the runtime adapter proof with Profile only. `assets/js/modules/profile-panel.js` now has a small visual-profile registry: Legacy remains the default/fallback renderer, while Prototype 2027 rearranges the same Profile payload into account identity, metrics, action rows, edit form disclosure, and sign-out presentation. Existing `data-dsa-*` account hooks remain the authority for Orders, Downloads, Addresses, Password reset, Notification preferences, avatar upload, profile updates, email verification, and logout. `assets/css/surface.css` contains the scoped Prototype profile styling, and `assets/js/surface.js` passes the visual profile plus safe local metrics into the adapter. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 3 extends the visual-profile adapter proof to Menu and Saved. `assets/js/modules/surface-panels.js` now keeps Legacy renderers as fallback/default while allowing Prototype 2027 renderers for Menu and Saved. Menu keeps the page table-of-contents and admin dashboard action using the same `data-dsa-menu-anchor` and `data-dsa-full-navigation` contracts. Saved keeps the same saved-item open/remove selectors while adding Prototype summary metrics and shelf-style grouping. Styling remains scoped to `.dsa-visual-prototype` in `assets/css/surface.css`. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 4 extends the visual-profile adapter proof to Cart only, leaving Checkout/payment untouched. `assets/js/modules/commerce-panels.js` now keeps the Legacy Cart renderer as fallback/default and adds a Prototype 2027 Cart renderer for the same payload. Quantity controls, product navigation, FBT add/claim actions, cart mutation queue, checkout-open handoff, discount summary, and trust badges retain their existing selectors and authority. `assets/js/surface.js` passes the visual profile and protected trust badges into the Cart adapter, and `assets/css/surface.css` scopes the Prototype cart layout to `.dsa-visual-prototype`. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 5 extends the visual-profile adapter proof to the Search shell. `assets/js/surface.js` now keeps the Legacy Search shell as fallback/default and adds a Prototype 2027 shell that rearranges title, visible status, search field, filters, alphabet rail, and results container while preserving the same required `data-dsa-search-*` slots. `assets/js/search.js` remains the behavior authority for Search REST requests, Bricks Filter Search bridge reconciliation, filter state, alphabet traversal, quick-add, and result rendering. Styling remains scoped to `.dsa-visual-prototype` in `assets/css/surface.css`. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 6 extends the visual-profile adapter proof to Links, notification preferences, and the iOS install guide. `assets/js/modules/links-panel.js` now keeps the Legacy Links renderer as fallback/default and adds a Prototype 2027 renderer for identity, score, social links, shop/cart actions, posts, review, and health/trust blocks. `assets/js/modules/surface-panels.js` now keeps Legacy notification/iOS renderers as fallback/default and adds Prototype 2027 wrappers around the same notification form and install-step selectors. `assets/js/surface.js` passes the active visual profile into Links, Notifications, and iOS payloads. Styling remains scoped to `.dsa-visual-prototype` in `assets/css/surface.css`. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 7 extends the visual-profile adapter proof to AI panel, inbox, and report presentation only. `assets/js/modules/ai-panel.js` now keeps Legacy AI renderers as fallback/default and adds Prototype 2027 renderers for panel title, signal inbox, empty state, report grid, and disabled chat placeholder. `assets/js/surface.js` passes the active visual profile into full AI payloads, inbox rerenders, and report rendering. Existing insight action, insight dismiss, notification dismiss, tray toggle, swipe, popout, and AI action executor logic remain core-owned. Styling remains scoped to `.dsa-visual-prototype` in `assets/css/surface.css`. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 8 adds the first runtime theme registry bridge. `assets/js/surface.js` now exposes `window.DSA.ui` with the active UI contract id, adapter version, active visual profile, adapter-ready screen ids, Active/Hover color model, and profile-aware payload helpers. Repeated screen payload lookups now use the central visual-profile helper instead of each panel rediscovering `data-dsa-visual-profile`. This keeps Legacy and Prototype 2027 on the same universal token/geometry/runtime contract and prevents prototype-only assumptions from becoming hidden dependencies. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 9 defines the marketplace theme-package boundary without importing arbitrary third-party code. `ui-system/theme-manifest.schema.json` now describes a `kiwe.surface-theme.v1` manifest with required UI/token contracts, scoped CSS/assets, supported presentation modes, budgets, and forbidden authority classes. `ui-system/marketplace-package.md` documents import/export rules, rejection checks, the `window.DSA.ui` bridge, and designer handoff expectations. Marketplace themes remain CSS/token/profile-led and cannot ship PHP, remote scripts, trackers, service workers, Bricks template imports, or alternate cart/checkout/PhoneKey/Search/geometry/history authority. No version bump, package rebuild, or manifest rehash was performed in this batch.

Batch 10 adds the first mechanical marketplace acceptance gate. `tools/ui-theme/validate-package.cjs` validates proposed theme folders against `ui-system/theme-manifest.schema.json` and `screen-payloads.json`, rejects forbidden executable/document files, unsafe relative paths, unlisted files, remote CSS imports/URLs, data/javascript URLs, and CSS budget overages, and checks that declared screens are known DSA surfaces. `tools/ui-theme/fixtures/valid/` provides a safe package fixture for the validator. No version bump, package rebuild, or manifest rehash was performed in this batch.

Release hygiene for the UI-brain/theme-marketplace track was refreshed at `0.5.65` after the Legacy UI review handoff was added. The MU loader and package entry versions are synchronized, and `tools/release/build-package-manifest.cjs` regenerated the package manifest for the folder-based MU deployment. No ZIP was produced because this installation is managed as an MU plugin folder.

Batch 11 closes the first standalone-preview handoff gap discovered by the external Prototype V3 handoff. The importable Prototype V3 theme package validates successfully once the UI brain documents the existing Games surface. `ui-system/preview-handoff.md` now separates importable theme files from preview-only HTML/placeholders/simulator code, explains why preview controls must live outside the app viewport, requires production-style `data-dsa-*` selectors and Geometry Engine variables, and sets natural placeholder rules. `ui-system/preview/standalone-preview.example.html` provides a minimal copyable preview skeleton. The broken external preview is therefore treated as a preview-harness failure, not evidence that the runtime theme engine is broken.

Batch 12 audits the final external V3 handoff and tightens the dock-theme boundary. The importable V3 package passes the source validator and stays within the CSS budget, but its preview/import CSS does not yet explicitly exercise the production split-dock state despite claiming split dock support. The UI contract now names `split-dock` and `full-dock` support separately from generic Dock and Navigation bar support, and the preview handoff contract documents the required `dsa-dock-split` root class plus `is-split-*` button classes. Admin ownership is corrected: Kiwe > Theme now owns dock presentation, split/full compact dock, Navigation bar, material/shape, and responsive dock placement; Kiwe > Dock remains responsible for module order, visibility, and the optional legacy context rail.

Batch 13 audits the corrected external V3 dock-modes handoff and adds a matching Legacy UI review handoff. The V3 dock-modes package validates, stays within budget, declares full/split dock plus Navigation bar support, and its preview now uses production `dsa-dock-split` and `is-split-*` classes gated to compact Dock presentation. `ui-system/handoffs/legacy-ui-review/` gives reviewers a portable Legacy baseline manifest, standalone preview, placeholder notes, and neutral review brief so outside designers can independently find visual issues without being handed the whole plugin or a list of known defects.

Batch 14 fixes shared/core UI primitives reported from live Prototype usage. Split compact dock now preserves rounded segment corners when AI/action is followed or preceded by a single item, cart quantity controls use token-tinted surfaces instead of white pills, cart checkout and AI chat controls flow with panel content instead of sticking over products/messages, and sheet scrollbars are styled thin with transparent tracks and hidden WebKit arrow buttons so they do not visually cut into rounded sheet corners. Kiwe > Theme now owns a real `style[screen_heading_tag]` setting for top-level DSA screen titles, and the runtime normalizes only screen-title nodes so nested product, report, and content headings keep their own semantics. Release hygiene was bumped to `0.5.66` and the package manifest was regenerated for the folder-based MU deployment.

Batch 15 restores the FBT/upsell presentation as a core rail contract across themes. Prototype 2027 no longer overrides `.dsa-cart-fbt__rail` into an auto-fit grid; FBT remains a horizontal side-scrolling rail with overflow, touch momentum, and snap behavior in every visual profile and sheet/classic mode. The cart renderer now emits explicit `data-dsa-cart-fbt` and `data-dsa-cart-fbt-rail` markers, and the UI brain documents that themes may style FBT cards but must not reinterpret the rail as a stacked list or grid. Release hygiene was bumped to `0.5.67` and the package manifest was regenerated for the folder-based MU deployment.

Batch 16 tightens the Geometry/token/theme handoff after live Links and Menu regressions. Prototype Links identity now rebalances logo plus site-score instead of shrinking the score copy into unreadable fragments, and Prototype Shop/Cart action cards explicitly consume token text colors so light and dark modes remain readable. Menu table-of-contents anchor clicks now wait until the Surface scroll lock has actually released before marking the page scroll complete, fixing the “list loads but click does not scroll” race. The UI brain and main UI contract now state that mixed-content rows must wrap/rebalance before text becomes illegible and that top-level screen semantic title tags are Theme-owned through Kiwe > Theme’s “Screen title tag.” The Legacy UI review preview now uses the production `navbar` runtime value and labels split/navbar as shared core dock modes. Release hygiene was bumped to `0.5.68` and the package manifest was regenerated for the folder-based MU deployment.

Batch 17 graduates the modern built-in UI from prototype status to `Kiwe 2027`. Admin now saves the profile as `kiwe2027`, while existing saved `prototype` values remain compatibility aliases and still receive the same scoped CSS class so older installs do not break. Links site score is now truly optional from settings through REST/runtime hydration: blank score means no score badge, not `96`, `0`, or an empty white card, and a one-time settings migration clears the old generated `96` default so “not configured” sites do not keep showing a fake score. Kiwe 2027 full compact dock receives a sealed tokenized surface to prevent page content from visually bleeding through the dock, and the UI brain now names full compact dock, split compact dock, and Navigation bar as core shell modes a theme may style but not reinterpret. Release hygiene was bumped to `0.5.69` and the package manifest was regenerated for the folder-based MU deployment.

Batch 18 tightens shared dock/UI details after live Kiwe 2027 review. Dock shape is now a real core state with Pill, Rounded box, and Square / no radius options in Kiwe > Dock and Kiwe > Theme; the renderer normalizes the old `rounded` value to Pill for compatibility and publishes radius variables for full dock, split segments, controls, and context rail. Kiwe 2027 full compact dock now follows the modern pill design with dark active module treatment and neutral AI action styling, while split compact dock still uses the same segment classes but honors the selected shape. Links admin controls now render as proper pill buttons instead of tiny text links in both Legacy and Kiwe 2027. The UI brain and theme manifest schema now document/allow dock shape support markers. Release hygiene was bumped to `0.5.70` and the package manifest was regenerated for the folder-based MU deployment.

Batch 19 cleans up the portable UI-system handoff for outside AI/designer use. `ui-system/prompt.md` now tells an AI how to read the folder, what to design, what not to own, how to structure importable theme packages, and which shell states/screens must be previewed. `profiles/legacy.css` was added as a compact Legacy baseline reference beside `profiles/kiwe-2027.css`, while the standalone preview skeleton and Legacy review preview now expose current dock shape classes and variables for Pill, Rounded box, and Square / no radius. Release hygiene was bumped to `0.5.71` and the package manifest was regenerated for the folder-based MU deployment.

Batch 20 sharpens the AI theme-creation prompt for handoff use. `ui-system/prompt.md` now explicitly describes Kiwe DSA as a WordPress/WooCommerce AppShell theme target, gives a recommended file-reading order through the UI brain, requires a complete handoff folder rather than loose CSS/screenshots, and explains how to build a standalone preview with preview-only placeholders linked to the importable theme CSS. The marketplace example and validator fixture now include Navigation bar plus dock shape support markers. Release hygiene was bumped to `0.5.72` and the package manifest was regenerated for the folder-based MU deployment.

Batch 21 clarifies the token boundary for AI/designer handoffs. `ui-system/tokens-reference.md` now states that `token-map.css` is a curated portable theme-token surface rather than a raw dump of every production CSS variable, explains which runtime/internal variables are intentionally excluded, and gives a process for requesting promotion of a missing token into the stable UI brain. `prompt.md` now tells theme authors to read the token reference before designing. Release hygiene was bumped to `0.5.73` and the package manifest was regenerated for the folder-based MU deployment.

The runtime split remains: DSA owns authority, screen data, Woo/PhoneKey/Bricks contracts, geometry, and lifecycle; themes own safe arrangement and presentation. For now marketplace color authority remains limited to the existing active and hover colors, with broader palette controls deferred.

## Active Lead Log - 2026-07-13

Runtime/package version is now `0.5.61` for the profile-neutral Sheet geometry and split-dock correction pass. Sheet spacing/origin/width controls work for both Legacy and Prototype 2027, split dock is constrained to Dock presentation only, Navigation bar AI contrast is corrected, and the prototype `ui/` folder remains reference-only. No ZIP is required for deployment.

The current sequence is:

| Batch | Scope | Status | Acceptance boundary |
|---:|---|---|---|
| 0 | Non-deferred hardening before htmx/Alpine | Code complete; verification limited by missing local PHP/Node/Git binaries | JSON-LD script breakout, browser debug leakage, service-worker opt-out, cart/checkout/panel async races, and SecureTrack optional-surface containment are addressed. PhoneKey auth/state-machine and privacy/master switches are explicitly deferred by product decision. |
| 1 | htmx/Alpine foundation | Code complete; verification limited by missing local PHP/Node/Git binaries | Libraries are introduced through WordPress-owned enqueue gates, disabled by default until contracts pass, with no ownership over PhoneKey, checkout/payment authority, service-worker policy, or core Surface lifecycle. |
| 2 | htmx server-fragment pilots | Code complete; verification limited by missing local PHP/Node/Git binaries | Low-risk admin and read-mostly Surface fragments move to server-owned partials with nonce/mutation contracts preserved. First pilot: Developer package-proof fragment. |
| 3 | Alpine local-widget pilots | Code complete; verification limited by missing local PHP/Node/Git binaries | Only isolated local UI widgets use Alpine state; no persisted cross-account/account/cart/auth state moves into Alpine. First pilot: Developer enhancement-gate preview state. |
| 4 | Cleanup, contracts, version bump, and package | Complete for source/package release; live proof remains external | Source boundary contract and docs are updated. Loader/package/cache-busting version is synchronized at `0.5.61`; manifest hashes may report drift until the local package is re-hashed, but required runtime-file verification remains the boot gate. |

This makes the integration a five-batch track including Batch 0. SecureTrack containment stayed inside Batch 0, so Batch 1 can begin without splitting the track.

## Release Position

The APEX architecture through S19 exists, but Kiwe is not yet a production-certified 1.0. The remaining work is no longer feature discovery. It is trust-boundary correction, cache/write-budget work, payload reduction, SEO/advertising compatibility, live platform proof, and repeatable release engineering.

The historical estimates of **96% implementation breadth** and **84% controlled readiness** described feature coverage before this pre-1.0 audit. The stricter release interpretation is now:

| Dimension | Complete | Remaining | Meaning |
|---|---:|---:|---|
| WordPress MVP feature breadth | 96% | 4% | Nearly every planned v1 workflow exists. |
| Pre-1.0 engineering closure | 97% | 3% | Core trust, cache isolation, bounded writes, PWA/Push worker contracts, runtime bootstrap, first-use presentation, SEO/advertising compatibility, WordPress 7 adapters, and reproducible package engineering are closed; live certification remains. |
| Production certification | 78% | 22% | Reproducible inventory and rollback instructions now exist. Real-host commerce, identity, Push vendors/browsers, Redis/no-cache, CDN/crawler/AdSense, incomplete-upload recovery, and executed rollback evidence remain incomplete. |
| Approved APEX architecture | 100% | 0% | Architecture presence is not production proof. |

## Confirmed Audit Decisions

| Finding or proposal | Decision | Release treatment |
|---|---|---|
| Mutation authorization accepts requests with no positive same-site signal | **Accept: blocker** | Require explicit mutation proof and preserve public/read-only routes separately. Do not rely on `Origin`/`Referer` alone. |
| Push unsubscribe lacks owner binding | **Accept: blocker** | Bind deletion to the current user/visitor subscription identity. |
| Reward minimum duration trusts client duration through `max()` | **Accept: blocker** | Minimum play time uses server elapsed time. Client scores remain decorative until stronger proof exists. |
| Profile email changes immediately | **Accept: blocker** | Pending-email verification and PhoneKey recovery-anchor reconciliation. |
| `DSA_DATA` embeds nonce, identity, cart, and other personalized state in cacheable HTML | **Accept: blocker** | Split static boot configuration from post-load private hydration; no personalized full-page cache leakage. |
| Abandoned-cart capture updates a row on every non-empty-cart pageview | **Accept: high** | Write only on cart-hash change or a bounded heartbeat. |
| Secret encryption key is salt-derived and salt rotation can orphan data | **Accept: high** | Add key-version diagnostics, explicit undecryptable-row handling, and recovery/re-enrolment behavior. |
| SecureTrack may store a `legacy:` base64 secret fallback | **Accept: blocker if reachable** | Remove plaintext-equivalent fallback; secret storage fails closed. Consolidation occurs through adapters, not a risky big-bang rewrite. |
| Per-IP fixed-window transient limiter writes option rows on no-cache hosts | **Accept: high** | Introduce a bounded atomic rate-limit store/token bucket and migrate hot paths. |
| Search floods transients for every query | **Reject as stale** | Current Search uses the WordPress object-cache API and versioned cache identity. Profile it, but do not solve an obsolete implementation. |
| Case-insensitive autoloader directory glob | **Accept: medium** | Normalize filenames first, instrument misses, then remove fallback. |
| Service worker performs an extra editorial REST fetch per eligible navigation | **Accept with scope** | Pilot is off by default. Add freshness throttling/save-data guards before certification. |
| Exact service-worker asset URL matching | **Accept: medium** | Normalize same-origin pathname/version matching without broad caching. |
| Root and package headers create two activatable plugins | **Reject literal claim; accept version drift** | WordPress MU loading only discovers the root loader. Keep the loader header, synchronize versions, and identify package/loader roles explicitly. |
| Admin bar is hidden unconditionally | **Accept: medium** | Gate it by enabled route and the explicit admin setting. |
| Settings migrations perform constructor I/O | **Accept: medium** | Move migrations to an explicit idempotent lifecycle step. |
| PhoneKey and SecureTrack should become independent products immediately | **Reject rewrite; accept boundaries** | Retain integrated modules for 1.0, strengthen bridge interfaces and stop new direct global calls. |
| Replace hand-written WebAuthn immediately | **Defer replacement; accept attack tests** | Run replay, RP/origin, UV, sign-count, recovery, and multi-device matrices first. A library migration requires its own compatibility review. |
| Move everything to enums/readonly/Interactivity/Abilities | **Reject wholesale modernization** | Apply only at stable internal boundaries and measured islands. External WP/Woo/Bricks payloads remain compatible arrays/hooks. |
| Action Scheduler for all background work | **Modified adoption** | Use it when Woo is present; retain a WordPress cron adapter for non-commerce sites. |
| Speculation Rules everywhere | **Deferred and route-gated** | Never speculate checkout, account, cart, auth, personalized, or protected routes. Editorial-only evaluation follows SEO/cache proof. |
| Background offline cart, Payment Request, fleet telemetry, partner/network work | **Post-1.0** | Useful platform ideas, not release closure. |

## 1.0 Execution Batches

Priority uses the established model:

`priority = trust impact + adoption impact + architecture unlock - implementation risk - context cost`

Sizes: **S** 5k-9k tokens, **M** 9k-16k, **L** 16k-26k. An L batch must be split if its verification surface grows.

| Order | Batch | Size | Acceptance boundary |
|---:|---|---:|---|
| 1 | **RC1 Mutation Trust Boundary** | M | **Complete in 0.5.12.** Every DSA/PhoneKey REST state change now requires the non-simple `X-Kiwe-Mutation` proof header while cross-site Fetch Metadata/Origin remains rejected. Authenticated routes retain WordPress nonce/capability identity. Push unsubscribe is user/visitor-owned, anonymous subscriptions require a real Kiwe visitor identity, Bricks and Surface clients send the contract, and admin AJAX/admin-post paths were verified to retain their native nonce/capability checks. Background worker renewal no longer performs an ownership-unprovable delete; RC7 owns its safe renewal lifecycle. |
| 2 | **RC2 Rewards and Commerce Abuse Safety** | M | **Complete in 0.5.13.** Minimum play time now uses server elapsed milliseconds only; client duration is telemetry. Attempt tokens are atomically consumed, identity/IP ledgers serialize completion, scores are explicitly untrusted/decorative, and an admin-governed atomic daily coupon budget caps issuance. Reverse pairs retain one sorted coupon identity; cart/session reconciliation removes duplicate or overlapping affected-product claims. A source-contract runner plus 100,000 randomized pair/scope cases passes. Live Woo tax, gateway, coupon, refund, and concurrency proof remains RC13. |
| 3 | **RC3 Identity and Recovery Integrity** | M/L | **Code complete in 0.5.14.** Profile email edits now remain pending until a one-time code sent to the new address is accepted; pending addresses are encrypted, tokens are atomically single-use, and stale requests cannot overwrite a newer anchor. Privileged email changes require the current WordPress password. Successful anchor rotation reconciles the verified PhoneKey factor, revokes remembered devices/IP trust and outstanding recovery/step-up challenges, destroys other WordPress sessions, and preserves enrolled passkeys. Existing new-device OTP-to-passkey enrollment and multi-credential storage were retained; privilege elevation and revocation now clear stale assurance. The PhoneKey account UI sends the shared mutation proof. A 17-contract identity/WebAuthn runner passes; real devices, SMTP inboxes, TOTP/backup recovery, and role/session behavior remain RC13 certification. |
| 4 | **RC4 Secrets and Crypto Recovery** | M | **Code complete in 0.5.15.** New shared, PhoneKey, and SecureTrack secret writes use versioned authenticated encryption and fail closed; plaintext-equivalent `b64:`/`legacy:` formats remain read-only migration inputs. The shared store reports version, non-secret key ID, algorithms, and configured recovery-key count, and supports explicitly supplied previous keys. PhoneKey records its salt-derived HMAC key ID and raises a production-critical mismatch instead of silently splitting identity. Push subscriptions now record the VAPID key ID; undecryptable VAPID material rotates deliberately, marks active devices `reenroll_required`, and browsers replace subscriptions whose application-server key/key ID changed. Admin/readiness diagnostics report crypto readiness and re-enrollment without revealing secrets. A 16-contract crypto/redaction runner passes; actual salt recovery, OpenSSL/Sodium host matrix, and browser re-enrollment remain RC13/RC14 evidence. |
| 5 | **RC5 Cache-Safe Boot Contract** | L | **Code complete in 0.5.16.** The reusable shell now carries public site/theme/route/module geometry only: REST nonce, profile/cart badges, PhoneKey identity, protected-flow state, personalized commerce, admin dashboard/Secure/AI capability, and Links editor authority hydrate through a same-site `/runtime/hydrate` response marked private/no-store and varied by cookie. Dock modules render neutrally, then role visibility and geometry reconcile after hydration. Bricks cart clients carry no inline nonce and acquire one immediately before mutation. A deterministic boot seed and 19-contract cache runner pass. Live anonymous/logged-in CDN reuse, Woo session cookies, cache-plugin exclusions, and account-page bypass remain RC10/RC13/RC14 evidence. |
| 6 | **RC6 Shared-Host Write Budget** | M/L | **Code complete in 0.5.17.** REST and Store Analytics throttles now use one shared atomic limiter: persistent object-cache increments on Redis/Memcached, otherwise a compact SQL table with one row per logical bucket, atomic upsert, hourly expiry cleanup, and fail-open storage recovery. Unchanged abandoned carts skip writes inside a configurable five-minute heartbeat and then update only identity/activity fields; real cart changes still write immediately, same-request duplicates collapse, and cleanup includes retained abandoned rows even after tracking is disabled. Store Analytics gains daily 180-day cleanup, Metrics context cardinality is capped, and analytics/cart/metrics/saved/reward writes emit opt-in runtime profiles. Readiness reports limiter backend/table/cron health. An 18-contract runner plus a 100,000-request bounded-row model passes. Live Redis and no-object-cache traces remain RC13/RC14 evidence. |
| 7 | **RC7 PWA, Push, and Worker Efficiency** | M | **Code complete in 0.5.18.** Push subscriptions now receive a random, one-way-stored renewal capability bound to the issuing endpoint. Worker-only `pushsubscriptionchange` must present that capability and old endpoint; the server rotates it while preserving owner identity, and stale browser metadata gets one identity-owned recovery save. Unsubscribe clears browser/worker renewal state. The worker no longer caches arbitrary same-origin images, uses strict pathname/version matching for shell assets, skips editorial/media work under Save-Data, throttles offline editorial refresh to 15 minutes with deterministic cache timestamps, retains route/cache-policy exclusions, bounds caches, and constrains notification clicks to same-origin. Push batches use expiring execution locks, stale jobs are purged, and 404/410 vendor responses expire devices immediately. Existing VAPID/OpenSSL/cron/readiness diagnostics remain authoritative. A 23-contract suite independently parses the generated worker. Real browser renewal, vendor delivery, denied/revoked permissions, and host cron remain RC13/RC14 evidence. |
| 8 | **RC8 Runtime Boundaries and Bootstrap** | M | **Code complete in 0.5.19.** Loader and package versions now fail open on mismatch. Required-file verification moved from dozens of unconditional probes and an exception to a release-stamped, 12-hour cached package manifest with missing-file diagnostics and a non-fatal request shutdown. Safety migrations run explicitly before settings resolve rather than during construction. Every autoloaded class filename matches its namespace exactly, the directory-scanning case fallback is removed, and misses are reported once per request. Frontend admin-bar hiding is a Dock setting applied only while the Kiwe shell is enabled on an eligible frontend route. An executable boundary freezes existing legacy PhoneKey/SecureTrack global consumers so new services must enter through adapters; the existing debt remains targeted rather than hidden by a risky rewrite. Eleven RC8 contracts pass. Live partial-upload, cache-option failure, rollback, and direct-loader drills remain RC12/RC14 evidence. |
| 9 | **RC9 Thin Surface and UI Contract Closure** | L split | **Complete in 0.5.22, payload-corrected in 0.5.50, geometry-corrected in 0.5.51, token-aligned in 0.5.52, behavior-corrected in 0.5.53.** RC9A moved Cart/Checkout markup into one first-use module. RC9B moved Links/view-editor and eligible AI panel/inbox/report markup into pure first-use modules while state, actions, queues, popouts, persistence, lifecycle, geometry, and server authority remain resident. Version 0.5.50 additionally moves Menu, Saved, Games panel, notifications, iOS install, and dynamic Appsite Home markup into pure `surface-panels.js`; 0.5.51 restores Sheet grabber centering, visual-viewport sizing, inactive full-height side-nav scroll gestures, and relocated Profile context actions. Version 0.5.52 aligns headings, semantic colors, font stacks, reduced motion, and admin accent tokens without changing geometry ownership. Version 0.5.53 restores first cart-add confetti, centralizes dismissible AI/push/permission notifications, and makes Sheet checkout start at the scroll origin so WooCommerce first fields remain reachable. RC9 contracts cover these rules, and the marketplace harness passes 71 responsive light/dark horizontal/vertical variants with zero geometry or accessibility failures. The remaining right/left full-height Navigation bar scroll/click interference is parked for a named geometry-strategy pass, not counted as fixed. Live assistive-technology and third-party-theme proof remains RC13/RC14. |
| 10 | **RC10 SEO, AdSense, and Cache Compatibility Gate** | M/L | **Code complete in 0.5.23.** Non-document requests never receive the shell. New installs keep full-screen first-session Home off unless deliberately enabled; no-JS visitors cannot be stranded, Surface copy is `data-nosnippet`, and Home no longer adds a competing H1. Schema emits only on public indexable routes, canonicalizes IDs, strips campaign parameters, uses stable archive cache identity, defers fully to established SEO plugins, and defers Product schema to Woo. View Transition CSS is emitted only on server-approved editorial documents; ads and common consent containers bypass DSA navigation, while morph delivery remains off. Eighteen contracts pass. Live Google rendering, Rich Results, AdSense, consent platforms, CDN/cache plugins, CWV and crawler parity remain RC13/RC14 evidence. |
| 11 | **RC11 WordPress 7 Native Adapters** | M | **Code complete in 0.5.24.** Kiwe registers one official Ability category and two `manage_options`, readonly, schema-validated, core-REST-discoverable abilities: deterministic trust audit and bounded route/registry summary. Outputs are flattened and capped; no raw settings, element content, visitor state, writes, or transport authority cross the boundary. WordPress 7 receives one `@wordpress/interactivity` script-module bridge for the existing AI/app/data event stores; partial hosts retain the three classic fallback bridges. Site identity Block Bindings remain read-only and REST registration remains explicit. Sixteen contracts pass. Write abilities, AI Client execution, DataViews replacement, and shell DOM ownership remain deferred. |
| 12 | **RC12 Quality and Package Engineering** | M | **Code complete in 0.5.25, hotfixed through 0.5.53.** Replaced the scaffold README with the real install/support/recovery contract; added `.editorconfig`, `.gitattributes`, changelog, compatibility declaration, and an upgrade/incomplete-upload/rollback runbook. A deterministic generator inventories all 126 production package files by byte length and SHA-256. Runtime verifies that full inventory only when its release stamp changes or the cached 12-hour proof expires, reports missing and changed files, and keeps WordPress fail-open. The 0.5.26 line introduced current-host same-site request tolerance for Hostinger temporary/staging domains; 0.5.50 reasserts cache-safe boot by keeping first-paint nonce, protected-flow, and commerce state neutral/public-only and moving live nonce, cart, protected-flow, and personalized commerce state back to private no-store hydration. The 0.5.51 corrective package bumps the loader/package/cache-busting version and records the Sheet/Profile/checkout contract repairs. Version 0.5.52 adds a token-alignment release with explicit UI regression contracts. Version 0.5.53 adds notification/confetti/Sheet checkout behavior contracts and bumps the package for deployment visibility. The 0.5.28 hotfix adds `DSA.healthCheck()` and `?dsa_debug=1` diagnostics for boot payload, REST nonce, cart/search/runtime endpoints, and lazy module asset availability on live hosts without logging cookie values. CI declares PHP 8.2-8.4 source lanes, runs the established static source contracts, JavaScript syntax checks, package verification, and the explicit Playwright UI dependency. RC12 contracts pass without locally running PHP lint. Real WP/Woo/Bricks, incomplete-upload, rollback execution, and live Hostinger health reports remain RC13/RC14 evidence rather than being overstated as CI proof. |
| 13 | **RC13 Live Commerce and Identity Certification** | L split | **RC13A public Hostinger preflight added and passed against the test site in 0.5.30; Search deployment diagnosis and recovery landed in 0.5.31; cache-safe cookie-authenticated browser hydration landed in 0.5.32; and Bricks 2.3.7 request ordering was harnessed in 0.5.33. Live browser reproduction on 0.5.34 proved the remaining close failure: Kiwe's programmatic synthetic-history cleanup leaked a `popstate` to Bricks after Search reconciliation. Version 0.5.35 closes that ownership boundary, blocks third-party routers from treating overlay cleanup as page navigation, and persists Search only after `surface:history:released`. RC13B's cookie-aware live anonymous-commerce runner passed 11/11 checks against the Hostinger test store on July 4, 2026 while that host was serving 0.5.32.** It discovered an addable product, obtained mutation proof, preserved a Woo session across requests, verified add/hash/fragments/separate read/quantity/removal, and confirmed keyed cleanup without creating an order. The current reusable shell contains no session nonce or personalized PhoneKey/cart/protected-flow/commerce state. Canonical 0.5.61 requires deployment and the same live commerce/identity/search traces. Real HPOS orders, gateways, tax, pair coupons, refunds, paid/refund Analytics, plus PhoneKey devices, SMTP inbox, proxy identity, session recovery, and app onboarding remain live evidence and are not claimed complete. |
| 14 | **RC14 Security, Runtime, and Release Certification** | L split | **RC14A public certification tooling is implemented in 0.5.35.** Its read-only runner checks document/feed/robots isolation, no-snippet and canonical behavior, service-worker scope/version, private hydration, deployed JavaScript, and production diagnostics state. The Hostinger test site passed the preceding 17/17 public boundary checks on 0.5.34, but frontend diagnostics remain enabled and therefore production quietness is not yet certified. SecureTrack monitor/enforce/break-glass, Push delivery/expiry, no-cache/Redis traces, browser/device/bfcache, fatal recovery, AdSense smoke, deployment, rollback, and the signed 1.0 release record remain. |

## Attempt Calculation

The calculated path is **14 focused batches**. RC1 through RC12 are code complete. Batches 13 and 14 may split when live evidence or blast radius demands it.

- Expected total: **18 attempts**; **5 remain after RC12**.
- Responsible range: **15-23 attempts** (the established +/-30% tolerance).
- Code-complete release candidate target: after RC12.
- Production-certified 1.0: only after RC13 and RC14 evidence is recorded.

### Theme and Admin Ownership Cleanup (`0.5.39`)

The optional style-preset experiment is retired. Neumorphic, Glassmorphism, Bold & Vibrant, and Minimal & Clean are not part of 1.0 because they created too much risk around geometry, contrast, and runtime ownership. `Kiwe > Theme` now exposes only two production presentation modes: Classic Surface and Sheet presentation. Both use the same modules, data, REST, PhoneKey, cart, search, AI, notification, dock, and geometry contracts.

Admin ownership is explicit and the historical `Kiwe > Surface` page is no longer menu-visible. The main Kiwe overview owns build/readiness status; App owns shell render state and PWA/permission journeys; Theme owns Classic/Sheet presentation and DSA runtime colors; Dock owns module order, compact Dock or Navigation bar geometry, material, and visibility; Games and Links own their respective module settings; Developer owns diagnostics, attributes, runtime cleanup, enhancement gates, export/reset, and deployment tooling. The legacy fragment renderer is removed; its controlled editorial morph successor exists behind a Developer gate and remains off until live proof. The former S18 generated delivery pilot is retired; packaged CSS remains the only live stylesheet authority.

### Dock Geometry Simplification (`0.5.40`)

The ambiguous full-axis Classic checkbox is removed. Version 0.5.43 introduces the separately named Navigation bar presentation with explicit edge selection, zero outer gap, internal safe-area padding, and geometry-engine cross-axis reserves. Compact Dock remains the default and no old `fill_axis` value independently controls runtime geometry.

### Sheet Continuity And Navigation Truth (`0.5.41`)

The bottom Sheet now owns one continuous surface through the viewport safe-area edge. Its module body reserves dock and context space inside that same material; the detached pseudo-tray and footer band are removed. Production Readiness now distinguishes critical blockers from outstanding proof warnings and describes navigation precisely: the unsafe legacy fragment renderer is deleted and hard-disabled; ordinary production navigation is full-document plus the transition Surface; the separate S13-S16 controlled editorial morph pipeline is implemented but remains Developer-gated until its live compatibility matrix passes.

## Parked and Excluded From the 1.0 Count

SEAM is intentionally parked by product decision. Its references stay bundled and documented, but **SEAM-1, SEAM-2, and SEAM-3 are not part of the 14-batch/18-attempt 1.0 calculation**. POS, restaurant/table ordering, customer score, partner SDK, marketplace, edge distribution, cross-site identity, background offline cart, and fleet telemetry are also post-1.0 product tracks.

S15 morphing and S17 offline editorial may ship in 1.0 as **off-by-default, clearly labeled pilots**. Their broad enablement is not required for 1.0 and must not delay the safe SSR/full-navigation baseline.

## Release Rules

- Trust and deterministic server authority win over novelty.
- No mutation depends only on ambient cookies plus optional browser headers.
- Anonymous HTML may be cached; personalized state must hydrate privately after load.
- SEO, accessibility, consent, advertising, and ordinary WordPress navigation remain correct with JavaScript unavailable.
- Checkout, payment, identity, security, and reward money never move into optimistic client authority.
- WordPress 7 APIs remain feature-detected adapters until stable on the supported matrix.
- Bricks integration stays additive and Bricks-owned for page design.
- Every experimental delivery pilot remains off by default until its own proof is complete.
- Historical decisions remain in `DSA-ARCHITECTURE.md`; rejected ideas remain documented so they are not repeatedly rediscovered.

## Admin Ownership And Navigation Bar (`0.5.43`)

The historical `Kiwe > Surface` placeholder is removed from the admin menu. The main `Kiwe` overview owns build/readiness status. `Kiwe > App` owns shell enablement, first-session Home, PWA install, adoption, and permission journeys. `Kiwe > Dock` owns destination order, visibility, compact Dock geometry, and the full-axis Navigation bar presentation. `Kiwe > Theme` owns Classic-only visual controls, Sheet-only controls, shared semantic colors, and transition presentation. `Kiwe > Games` and `Kiwe > Links` are first-class owner pages. `Kiwe > Developer` owns diagnostics, recovery, enhancement gates, export/reset, deployment tools, and builder-neutral attributes.

Dock presentation is now an explicit choice rather than the old ambiguous `fill_axis` checkbox. `Dock` remains compact and positionable. `Navigation bar` attaches to a selected top, right, bottom, or left viewport edge with no external gap, fills only its orientation axis, and keeps safe-area padding inside. Both presentations use the same registered modules, actions, badges, context authority, geometry variables, and accessibility contract.

Theme forms are ownership-safe. Classic visual settings are visible and submitted only for Classic; Sheet placement/backdrop/motion settings are visible only for Sheet. The renderer does not emit Classic glass/material/motion classes in Sheet mode. Partial App, Theme, Menu, and Dock saves preserve settings owned by other pages.
## UI Overhaul Foundation (0.5.43)

The external UI proposal was reviewed as an untrusted design branch. Kiwe adopted its tablet-profile and sheet-affordance goals but rejected its repeated selector stack and any HTMX/Alpine dependency. Runtime now emits normalized device-profile geometry, the Dock admin presents Desktop/Tablet/Phone cards, and Sheets use explicit handle/drag dismissal while preserving the shared lifecycle. Remaining UI work is tracked in KIWE-UI-OVERHAUL-ROADMAP.md.

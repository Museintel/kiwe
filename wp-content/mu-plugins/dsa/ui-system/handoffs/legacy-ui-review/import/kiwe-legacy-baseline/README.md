# Kiwe Legacy Baseline Manifest

This is a review manifest for the built-in Legacy UI profile.

It does not install a new theme and intentionally ships no CSS, JavaScript, or assets. Legacy is already bundled in Kiwe core.

Use it to validate that Legacy participates in the same UI contract as future marketplace themes.

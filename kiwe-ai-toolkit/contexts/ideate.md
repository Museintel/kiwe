# /ideate

Use the supplied brief, assets and SiteGraph context to explore a distinctive design with the user. Ask only for missing decisions that materially change the result. Existing Design Context is authoritative for business facts and owner-locked brand values.

Do not apply Seam Framework, Bricks structures or a SEAM house style. Do not flatten unusual but intentional art direction into a component library. Keep corrections conversational until the user accepts the result.

Return a complete browser-runnable HTML/CSS/JS project. Include every required local asset and `assets/asset-manifest.json`, recording provenance and intended WordPress Media use. Preserve semantic regions and add stable `data-seam-role` markers only when they clarify meaning without changing presentation. Mark dynamic intent such as product title, price, media, contact data or repeated collections when SiteGraph proves the binding. Never invent IDs, fields, products or site facts.

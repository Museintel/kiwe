# Valid Bricks binding plan fixture

This fixture proves the `kiwe.bricks-bindings.v1` contract without needing a real WordPress install.

It represents the smallest safe dynamic pass:

- one product rail in `website/bricks-paste.html`;
- one Bricks product query loop binding;
- one canonical Kiwe launcher;
- one visible menu-context section;
- one target `kiwe.site-graph.v1` snapshot.


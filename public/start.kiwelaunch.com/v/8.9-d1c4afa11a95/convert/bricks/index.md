# /convert /bricks

SEAM contract: 8.9

Binding-only handoff with sourcePolicy immutable, exact per-file targets, explicit field slots, source guards and repeat prototypes. Never rewrite source or emit Bricks JSON.

## Requires
- complete accepted raw HTML/CSS/JS project, kept unchanged
- SiteGraph when site-specific fields are requested

## Outputs
- bricks-bindings/kiwe-bindings.json
- binding report
